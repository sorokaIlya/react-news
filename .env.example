# Base URL of the REST API (no trailing slash). Required.
EXPO_PUBLIC_API_URL=https://your-api.example.com

# WebSocket endpoint for realtime updates. Required.
EXPO_PUBLIC_WS_URL=wss://your-api.example.com/ws
